{# TODO: abstract this condition in the model #}
{% if shipment.carrier in ['fancourier', 'sprinter'] %}
    {% import 'KonektCourierBundle::macros.html.twig' as macros %}

    {{ macros.modalPlaceholder }}

    {% set trackingContent = shipment.tracking ? macros.awbDetailsButton(shipment.tracking) : macros.awbCreateButton(shipment.id) %}

    {{ trackingContent }}
{% endif %}